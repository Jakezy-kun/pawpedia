<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY. Overrides src/main/res/xml/network_security_config.xml so
  the app on the Android emulator can reach a PHP API running on this computer:
    BREED_API_BASE_URL=http://10.0.2.2:8080/api
  (10.0.2.2 is the emulator's alias for the host machine; 127.0.0.1 would be
  the emulator itself.)

  Release builds never include this file and keep the stricter main config.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">dogbreeds.mooo.com</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
