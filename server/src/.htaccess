# Never serve these files over HTTP. They hold code and credentials, not
# pages. Better still, upload this folder outside public_html.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
